version: '3'

services:

    web1:
      image: myweb:v1
    web2:
      image: myweb:v2

    nginx:
       image: nginx
       ports:
         - "8080:80"
       volumes:
         - ./nginx.conf:/etc/nginx/nginx.conf
